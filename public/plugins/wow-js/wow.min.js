/* WOW.js - Minimal */
var WOW = function(config) {
  this.config = config || {};
};
WOW.prototype.init = function() {};
WOW.prototype.sync = function() {};
if (typeof jQuery !== 'undefined') {
  new WOW().init();
}
